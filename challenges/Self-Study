1) Describe the biggest difference between .forEach & .map.

	Map creates a new array while forEach only operates on the original array.

2) What is the difference between a function and a method?
	
	A Function is Global while a method is associated with a object.

3) What is closure?
	
	Closure is when the scope of the variables are enclosed to a privately within a function.

4) Describe the four rules of the 'this' keyword.
	
	Global within this scope "this" is used to describe window/console.
	New is the this that is creating an instance of a class
	Explicit wheneve this is explicitly called on an object prior to its use.
	Implicit refers to the this before calling a function  

5) Why do we need super() in an extended class?

	Accompanied with the extends keyword to call variables and method inside of the parent class.
